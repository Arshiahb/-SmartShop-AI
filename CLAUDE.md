# راهنمای دائمی توسعه SmartShop AI

این فایل استانداردهای لازم برای طراحی، پیاده‌سازی، تست و بازبینی پروژه را مشخص می‌کند و باید در تمام تسک‌های آینده رعایت شود.

## استانداردهای UI/UX و فرانت‌اند

### طراحی بصری

- از پالت رنگی مدرن، مینیمال و سازگار استفاده شود؛ رنگ‌های پیشنهادی Tailwind شامل `Slate`، `Indigo` و `Emerald` هستند.
- رنگ‌ها باید معنا و سلسله‌مراتب بصری مشخص داشته باشند:
  - `Slate` برای متن، پس‌زمینه و مرزبندی‌های خنثی.
  - `Indigo` برای اقدامات اصلی، لینک‌ها و هویت بصری.
  - `Emerald` برای موفقیت، وضعیت آماده و بازخورد مثبت.
- رابط کاربری باید خوانا، خلوت، سازگار با زبان فارسی و دارای کنتراست مناسب باشد.

### Responsive Design

- تمام صفحات و کامپوننت‌ها باید روی موبایل، تبلت و دسکتاپ قابل استفاده باشند.
- برای کنترل نقاط شکست از واریانت‌های Tailwind مانند `sm:`، `md:` و `lg:` استفاده شود.
- طراحی باید ابتدا برای صفحه کوچک انجام شود و سپس برای نمایشگرهای بزرگ‌تر گسترش یابد.
- Grid، Flexbox، فرم‌ها، جدول‌ها، Navbar و پنجره‌های شناور نباید در عرض‌های کوچک باعث اسکرول افقی ناخواسته شوند.

### تعامل و انیمیشن

- برای Hover، Focus و Click از انتقال‌های نرم استفاده شود:

```html
transition duration-200 ease-in-out
```

- انیمیشن‌ها باید کوتاه، هدفمند و غیرمزاحم باشند.
- وضعیت Focus برای کنترل‌های قابل استفاده با صفحه‌کلید حفظ شود.
- دکمه‌ها باید حالت‌های عادی، Hover، Focus، Disabled و Loading مشخص داشته باشند.

### استاندارد HTMX

- عملیات HTMX باید بدون Refresh کامل صفحه انجام شوند، مگر اینکه Refresh صریحاً ضروری باشد.
- برای هر عملیات غیرهمزمان، Loading Indicator قابل مشاهده تعریف شود.
- هنگام ارسال درخواست، دکمه یا کنترل مرتبط باید موقتاً Disabled یا در وضعیت Loading قرار گیرد.
- پس از موفقیت یا خطا، بازخورد سریع و قابل فهم به کاربر نمایش داده شود.
- از الگوهای `hx-indicator`، `aria-live` و پیام وضعیت مناسب استفاده شود.
- ناحیه‌ای که با HTMX به‌روزرسانی می‌شود باید Target مشخص و قابل پیش‌بینی داشته باشد.

### فاصله‌گذاری و تایپوگرافی

- Padding و Margin بر اساس مقیاس ۸ پیکسلی طراحی شوند؛ اولویت با مقادیر Tailwind مانند `p-2`، `p-4`، `p-6`، `p-8` و معادل‌های آن‌ها است.
- فاصله‌ها باید در تمام صفحات سازگار و قابل پیش‌بینی باشند.
- اندازه، وزن و ارتفاع خط متن باید سلسله‌مراتب مشخص داشته باشد.
- متن فارسی باید با `dir="rtl"` و تراز مناسب نمایش داده شود.

## استانداردهای Python و Django

### خوانایی و تایپ‌گذاری

- تمام توابع و متدهای جدید باید Type Hint داشته باشند.
- برای ورودی‌ها، خروجی‌ها، ساختارهای پیچیده و سرویس‌های عمومی از تایپ‌های دقیق استفاده شود.
- نام‌گذاری متغیرها، توابع، کلاس‌ها و ماژول‌ها باید توصیفی و مطابق قراردادهای Python/Django باشد.
- از کدهای مبهم، شرط‌های تودرتوی غیرضروری و منطق تکراری جلوگیری شود.

### مستندسازی

- تمام توابع عمومی، سرویس‌ها و ابزارهای قابل استفاده مجدد باید Docstring تمیز داشته باشند.
- Docstring باید هدف تابع، ورودی‌ها، خروجی و خطاهای مهم را توضیح دهد.
- تصمیم‌های معماری یا رفتارهای غیر بدیهی باید در کد یا مستندات پروژه ثبت شوند.

### معماری و سبک پیاده‌سازی

- منطق کسب‌وکار در Viewها انباشته نشود؛ برای منطق قابل استفاده مجدد از Service یا لایه دامنه استفاده شود.
- Viewها باید سبک، قابل تست و مسئول هماهنگی درخواست و پاسخ باشند.
- وابستگی بین اپلیکیشن‌ها حداقلی و جهت‌دار باشد.
- تنظیمات حساس فقط از Environment Variable خوانده شوند و در مخزن ثبت نشوند.
- خطاها باید به‌صورت کنترل‌شده مدیریت شوند و پیام داخلی حساس به کاربر نمایش داده نشود.

## بهینه‌سازی دیتابیس و ORM

- پیش از پایان هر تسک، کوئری‌های جدید از نظر تعداد و هزینه بررسی شوند.
- برای روابط ForeignKey و One-to-One از `select_related` استفاده شود.
- برای روابط Many-to-Many و Reverse ForeignKey از `prefetch_related` استفاده شود.
- از N+1 Query به‌صورت جدی جلوگیری شود.
- فهرست‌ها باید صفحه‌بندی داشته باشند و از واکشی بی‌دلیل داده‌های بزرگ جلوگیری شود.
- فقط فیلدهای لازم در QuerySet واکشی شوند، مگر اینکه خوانایی یا نیاز دامنه خلاف آن را ایجاب کند.
- فیلترها، مرتب‌سازی‌ها و Lookupهای پرتکرار باید با ایندکس مناسب بررسی شوند.

## تست و کنترل کیفیت

- پیش از اتمام هر تسک، تست‌های مرتبط با `pytest` اجرا و نتیجه آن گزارش شود.
- قابلیت‌های جدید باید تست واحد یا یکپارچه متناسب با ریسک خود داشته باشند.
- سناریوهای موفق، خطا، دسترسی غیرمجاز، داده خالی و مرزهای مهم پوشش داده شوند.
- تغییرات مدل باید با Migration معتبر همراه باشند.
- پیش از تحویل، این بررسی‌ها در صورت مرتبط‌بودن اجرا شوند:

```bash
python -m pytest -q
python manage.py check
ruff check .
ruff format --check .
```

- شکست تست یا ابزار کیفیت باید پیش از تحویل بررسی و یا به‌صورت شفاف گزارش شود.

## چک‌لیست پایان هر تسک

- [ ] رابط کاربری با پالت Slate/Indigo/Emerald و طراحی Responsive سازگار است.
- [ ] Hover، Focus، Click و Loading state بازخورد مناسب دارند.
- [ ] عملیات HTMX دارای Loading Indicator و پیام نتیجه هستند.
- [ ] فاصله‌گذاری‌ها از مقیاس ۸ پیکسلی پیروی می‌کنند.
- [ ] توابع جدید Type Hint و Docstring دارند.
- [ ] کوئری‌ها با `select_related` و `prefetch_related` بازبینی شده‌اند.
- [ ] تست‌های pytest اجرا و نتیجه ثبت شده است.
- [ ] `manage.py check` و Ruff در صورت مرتبط‌بودن موفق هستند.
